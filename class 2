-------------------------------------------

PROCEDURE valid(x)
{
  IF x ≥ 4 AND x ≤ 9
  {
    IF NOT(x = 6)
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [3, 4, 6, 8, 10]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF valid(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 1
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
-------------------------------------------

# 1

```text
PROCEDURE valid(x)
{
  IF x > 2 AND x < 9
  {
    IF NOT(x = 5)
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [1, 3, 5, 7, 9]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF valid(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 2
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `2` and `16`
B. `2` and `15`
C. `3` and `17`
D. `3` and `15`

**答案：A**

**解析：**
满足条件的是 3 和 7。

* 1 不满足，sum + 2 → 2
* 3 满足，count = 1，sum = 5
* 5 被排除，sum = 7
* 7 满足，count = 2，sum = 14
* 9 不满足，sum = 16

---

# 2

```text
PROCEDURE check(x)
{
  IF x MOD 2 = 0
  {
    IF x ≥ 4 AND x ≤ 10
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [2, 4, 7, 8, 12]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF check(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 1
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `2` and `15`
B. `2` and `14`
C. `3` and `16`
D. `3` and `15`

**答案：A**

**解析：**
满足条件的是 4 和 8。

* 2 不满足，sum = 1
* 4 满足，count = 1，sum = 5
* 7 不满足，sum = 6
* 8 满足，count = 2，sum = 14
* 12 不满足，sum = 15

---

# 3

```text
PROCEDURE special(x)
{
  IF x > 3 OR x = 1
  {
    IF NOT(x = 6) AND NOT(x = 8)
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [1, 2, 4, 6, 9]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF special(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 3
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `3` and `20`
B. `2` and `18`
C. `3` and `19`
D. `4` and `21`

**答案：A**

**解析：**
满足条件的是 1、4、9。

* 1 满足 → count 1, sum 1
* 2 不满足 → sum 4
* 4 满足 → sum 8
* 6 被排除 → sum 11
* 9 满足 → sum 20
  count = 3

---

# 4

```text
PROCEDURE keep(x)
{
  IF x ≥ 2 AND x ≤ 8
  {
    IF x MOD 2 = 1
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [2, 3, 5, 8, 9]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF keep(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 2
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `2` and `14`
B. `2` and `12`
C. `3` and `16`
D. `3` and `14`

**答案：A**

**解析：**
满足条件的是 3、5。

* 2 不满足 → sum 2
* 3 满足 → sum 5
* 5 满足 → sum 10
* 8 不满足 → sum 12
* 9 不满足 → sum 14
  count = 2

---

# 5

```text
PROCEDURE valid(x)
{
  IF x < 10
  {
    IF x MOD 2 = 0 OR x = 3
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [3, 4, 7, 10, 8]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF valid(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 1
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `3` and `16`
B. `3` and `17`
C. `4` and `18`
D. `2` and `15`

**答案：B**

**解析：**
满足条件的是 3、4、8。

* 3 满足 → sum 3
* 4 满足 → sum 7
* 7 不满足 → sum 8
* 10 不满足 → sum 9
* 8 满足 → sum 17
  count = 3

---

# 6

```text
PROCEDURE choose(x)
{
  IF x > 1 AND x < 9
  {
    IF NOT(x MOD 2 = 0)
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [1, 2, 5, 7, 10]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF choose(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 2
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `2` and `16`
B. `2` and `18`
C. `3` and `17`
D. `3` and `19`

**答案：A**

**解析：**
满足条件的是 5、7。

* 1 不满足 → sum 2
* 2 不满足 → sum 4
* 5 满足 → sum 9
* 7 满足 → sum 16
* 10 不满足 → sum 18

等等，这里算出来是 18，不是 16。
修正选项：

A. `2` and `16`
B. `2` and `18`
C. `3` and `17`
D. `3` and `19`

**正确答案：B**

---

# 7

```text
PROCEDURE test(x)
{
  IF x = 2 OR x = 4 OR x = 6
  {
    RETURN(true)
  }
  ELSE
  {
    IF x > 7
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
}

nums ← [2, 3, 6, 8, 1]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF test(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 2
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `3` and `18`
B. `3` and `19`
C. `4` and `20`
D. `2` and `16`

**答案：B**

**解析：**
满足条件的是 2、6、8。

* 2 满足 → sum 2
* 3 不满足 → sum 4
* 6 满足 → sum 10
* 8 满足 → sum 18
* 1 不满足 → sum 20

这里又得到 20。修正选项：

A. `3` and `18`
B. `3` and `20`
C. `4` and `20`
D. `2` and `16`

**正确答案：B**

---

# 8

```text
PROCEDURE allowed(x)
{
  IF x ≥ 3 AND x ≤ 9
  {
    IF x = 4 OR x = 8
    {
      RETURN(false)
    }
    ELSE
    {
      RETURN(true)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [3, 4, 6, 8, 9]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF allowed(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 1
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `3` and `19`
B. `2` and `17`
C. `3` and `20`
D. `4` and `21`

**答案：A**

**解析：**
满足条件的是 3、6、9。

* 3 满足 → sum 3
* 4 被排除 → sum 4
* 6 满足 → sum 10
* 8 被排除 → sum 11
* 9 满足 → sum 20

这里是 20，不是 19。修正选项：

A. `3` and `20`
B. `2` and `17`
C. `3` and `19`
D. `4` and `21`

**正确答案：A**

---

# 9

```text
PROCEDURE good(x)
{
  IF x MOD 2 = 1
  {
    IF x > 2 AND x < 8
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    IF x = 10
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
}

nums ← [1, 3, 6, 7, 10]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF good(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 2
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `3` and `22`
B. `3` and `24`
C. `2` and `20`
D. `4` and `25`

**答案：A**

**解析：**
满足条件的是 3、7、10。

* 1 不满足 → sum 2
* 3 满足 → sum 5
* 6 不满足 → sum 7
* 7 满足 → sum 14
* 10 满足 → sum 24

这里是 24。修正选项：

A. `3` and `24`
B. `3` and `22`
C. `2` and `20`
D. `4` and `25`

**正确答案：A**

---

# 10

```text
PROCEDURE valid(x)
{
  IF x > 0 AND x < 10
  {
    IF NOT(x = 2) AND NOT(x = 9)
    {
      RETURN(true)
    }
    ELSE
    {
      RETURN(false)
    }
  }
  ELSE
  {
    RETURN(false)
  }
}

nums ← [2, 4, 6, 9, 11]
i ← 1
count ← 0
sum ← 0

REPEAT 5 TIMES
{
  IF valid(nums[i])
  {
    count ← count + 1
    sum ← sum + nums[i]
  }
  ELSE
  {
    sum ← sum + 3
  }

  i ← i + 1
}

DISPLAY(count)
DISPLAY(sum)
```

What is displayed?

A. `2` and `19`
B. `2` and `20`
C. `3` and `21`
D. `3` and `19`

**答案：B**

**解析：**
满足条件的是 4、6。

* 2 被排除 → sum 3
* 4 满足 → sum 7
* 6 满足 → sum 13
* 9 被排除 → sum 16
* 11 不满足 → sum 19

这里是 19。修正选项：

A. `2` and `19`
B. `2` and `20`
C. `3` and `21`
D. `3` and `19`

**正确答案：A**

















